# manual-pe-loader
Manual PE loader for Windows (research project). Demonstrates how native PE files are manually mapped, relocated, linked, initialized (TLS, SEH), and executed in memory without relying on the default Windows loader. Educational focus on PE internals and NT behavior.
